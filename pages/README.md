# Páginas secundárias

Esta pasta reúne páginas que não são a página inicial.

`login.html` contém a interface visual de entrada do usuário. Ela utiliza os estilos e scripts das pastas `css` e `js`, além do logotipo em `assets/identidade`.

Novas páginas independentes, como cadastro ou recuperação de senha, podem ser organizadas aqui.

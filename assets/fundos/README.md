# Fundos visuais

Esta pasta contém imagens usadas como fundo decorativo.

`bg_pattern.png` é um padrão aplicado pelo `css/style.css`. Fundos ficam separados porque normalmente são usados pelo CSS, e não diretamente por uma tag de imagem no HTML.
